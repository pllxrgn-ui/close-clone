/** DSL grammar version (CONTRACTS §C3 is normative at 1.0.0). */
export const GRAMMAR_VERSION = '1.0.0';

/**
 * Back-compat alias consumed by the shared package smoke test. Prefer
 * {@link GRAMMAR_VERSION} in new code.
 */
export const DSL_GRAMMAR_VERSION = GRAMMAR_VERSION;
